//
// Created by Luminous on 2024/12/24 13:50
//

#pragma GCC optimize(3)

#include <bits/stdc++.h>

class Solution {
public:
    int eatenApples(std::vector<int> &apples, std::vector<int> &days) {
//        const int n = (int) apples.size();
//
//        std::vector<std::array<int, 2>> v;
//        for (int i = 0; i < n; i++) {
//            v.push_back({i + days[i], apples[i]});
//        }
//
//        std::sort(v.begin(), v.end());
//
//        int ans = 0, before = -1;
//        for (auto &[day, apple] : v) {
//            if (before + 1 >= day) continue;
//            int cnt = std::min(apple, day - before - 1);
//            ans += cnt;
//            before += cnt;
//        }
//
//        return ans;
    }
};